# App Favicon Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Publish the supplied grocery-cart PNG as the browser tab icon for the Singapore Grocery Price Tracker.

**Architecture:** Use the Next.js App Router file convention by placing the exact source image at `src/app/icon.png`. A focused filesystem test validates the asset contract without adding an image-processing dependency.

**Tech Stack:** Next.js App Router, TypeScript, Vitest, PNG

## Global Constraints

- Preserve the supplied 512 x 512 transparent PNG without redrawing or changing its colors.
- Use `src/app/icon.png`; do not add manual metadata links or a legacy ICO.
- Run the repository's complete required verification before deployment.

---

### Task 1: Install And Verify The App Icon

**Files:**
- Create: `tests/app-icon.test.ts`
- Create: `src/app/icon.png`

**Interfaces:**
- Consumes: Next.js App Router metadata file convention.
- Produces: `/icon.png` metadata and browser favicon output generated by Next.js.

- [ ] **Step 1: Write the failing asset contract test**

```typescript
import { readFileSync } from "node:fs";
import { describe, expect, it } from "vitest";

describe("app icon", () => {
  it("provides the approved square PNG through the App Router", () => {
    const icon = readFileSync("src/app/icon.png");

    expect(icon.subarray(0, 8)).toEqual(
      Buffer.from([137, 80, 78, 71, 13, 10, 26, 10])
    );
    expect(icon.readUInt32BE(16)).toBe(512);
    expect(icon.readUInt32BE(20)).toBe(512);
  });
});
```

- [ ] **Step 2: Run the focused test and verify RED**

Run: `npx vitest run tests/app-icon.test.ts`

Expected: FAIL with `ENOENT` for `src/app/icon.png`.

- [ ] **Step 3: Install the approved image**

Copy `/var/folders/kj/3c05hc15085dbxc8s_m1f6340000gn/T/codex-clipboard-0199f1da-31a6-4cc7-a8d0-ca431a52ded9.png` byte-for-byte to `src/app/icon.png`.

- [ ] **Step 4: Run focused and full verification**

Run:

```bash
npx vitest run tests/app-icon.test.ts
npm test
npm run typecheck
npm run lint
npx prisma validate
```

Expected: the focused test and complete suite pass, typecheck and lint exit cleanly, and Prisma reports a valid schema.

- [ ] **Step 5: Commit and push**

```bash
git add src/app/icon.png tests/app-icon.test.ts docs/superpowers/plans/2026-06-22-app-favicon.md
git commit -m "feat: add grocery cart favicon"
git push origin main
```

- [ ] **Step 6: Verify production**

Wait for GitHub CI and Vercel production deployment. Confirm production HTML contains an icon link and its resolved URL returns `200` with an image content type.
